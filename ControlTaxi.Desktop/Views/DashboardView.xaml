<UserControl x:Class="ControlTaxi.Desktop.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24">
            <Border Background="#FFDFEAF5" BorderBrush="#FF98ADC3" BorderThickness="1" Padding="24">
                <StackPanel>
                    <Border Background="White" BorderBrush="#FFB4C5D7" BorderThickness="0,0,0,1" Padding="12" Margin="-24,-24,-24,20">
                        <TextBlock Text="ADMINISTRACION" HorizontalAlignment="Center" FontSize="22" FontWeight="Bold" Foreground="#FF263A53" />
                    </Border>

                    <StackPanel HorizontalAlignment="Center" Margin="0,0,0,28">
                        <TextBlock Text="CONTROL DE TAXI" FontSize="34" FontWeight="Bold" Foreground="{StaticResource AccentDarkBrush}" HorizontalAlignment="Center" />
                        <TextBlock Text="Sistema Desktop local sin navegador" Foreground="{StaticResource MutedBrush}" HorizontalAlignment="Center" />
                    </StackPanel>

                    <ItemsControl ItemsSource="{Binding Modules}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel HorizontalAlignment="Center" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Command="{Binding OpenCommand}"
                                        Width="250"
                                        Height="150"
                                        Margin="10"
                                        Background="White"
                                        Foreground="{StaticResource InkBrush}"
                                        BorderBrush="#FFCBD8E6"
                                        BorderThickness="1">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>
                                        <Border Width="58" Height="58" CornerRadius="29" Background="#FFEAF1FF" HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding Icon}" Foreground="{StaticResource AccentDarkBrush}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                        <TextBlock Grid.Row="1" Text="{Binding Title}" TextAlignment="Center" FontSize="15" FontWeight="Black" Margin="0,12,0,4" TextWrapping="Wrap" />
                                        <TextBlock Grid.Row="2" Text="{Binding Description}" TextAlignment="Center" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" />
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
