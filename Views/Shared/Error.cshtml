@{
    ViewData["Title"] = "Error";
}

<div class="page-shell">
    <div class="hero-card">
        <div>
            <p class="eyebrow">CONTROL TAXI WEB</p>
            <h1>Ocurrio un error en el portal</h1>
            <p>Revisa la conexion, la base seleccionada o vuelve al dashboard.</p>
        </div>
        <a asp-controller="Portal" asp-action="Dashboard" class="primary-link">Volver al inicio</a>
    </div>
</div>
